template:
  name: Repro Step
  identifier: repro_step
  versionLabel: v1
  type: Step
  spec:
    type: ShellScript
    spec:
      shell: Bash
      onDelegate: true
      source:
        type: Inline
        spec:
          script: |-
            echo "Successfully fetched template from branch!"
            # This exit 1 forces the failure needed to test the 'Retry' button
            exit 1